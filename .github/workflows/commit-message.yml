name: "Commit Message Check"
on:
  pull_request:
    types:
      - opened
      - edited
      - reopened
      - synchronize
  push:
    branches:
      - main

permissions:
  contents: read
  pull-requests: read

jobs:
  check-commit-message:
    uses: Slicer/Slicer/.github/workflows/commit-message.yml@c28ce0c16593fd6c494ceef4f65e3e4ee2d4d65c # main
